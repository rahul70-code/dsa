function BFS(graph, root) {
    let nodeLen = {};
    for(let i=0;i<graph.length;i++) {
        nodeLen[i] = Infinity;
    }

    nodeLen[root] = 0;
    let queue = [root];
    let current;
    while(queue.length != 0) {
        
    }
}